# Base URL of the API, including the /api suffix.
# Development: leave as /api — the Vite dev server proxies it to http://localhost:5014.
# Production: set the absolute backend URL in the host dashboard, e.g.
#   VITE_API_BASE_URL=https://your-backend.monsterasp.net/api
VITE_API_BASE_URL=/api
